// Copy this file to `Secrets.local.xcconfig` and fill real values.
// `Secrets.local.xcconfig` is gitignored and is used by both Debug and Release builds.

SUPABASE_URL = https:$(SLASH)$(SLASH)<your-project-ref>.supabase.co
SUPABASE_ANON_KEY = <your-anon-public-key>
